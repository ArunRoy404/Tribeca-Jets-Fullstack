import TripRequestsPage from "@/templates/TripRequestsPage";

export const metadata = {
  title: "Trip Requests | Tribeca Jets",
  description:
    "Every enquiry the desk has taken — the ones being worked, and the ones that never booked.",
};

export default function Page() {
  return <TripRequestsPage />;
}
