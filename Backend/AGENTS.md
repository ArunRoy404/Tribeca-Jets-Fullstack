# Backend — NestJS 12, TypeScript, ESM

**The rules live in the root `AGENTS.md`**, part two. This file carries no rules
of its own; it used to, and a rule that exists in two places is a rule that
disagrees with itself eventually.
